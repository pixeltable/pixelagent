# Pixel Agent

Build-your-own agent framework with Pixeltable.

## Installation

```bash
pip install pixelagent
```

## Usage

```python
from pixelagent import Agent

agent = Agent()
agent.run("Hello!")
```
